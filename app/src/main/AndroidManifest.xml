<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aaebrahimian.vaxino">
    
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vaxino">

        <activity android:name=".splashscreen.WelcomeSplash"
            android:theme="@style/splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activitycodes.ClinicActivity" />
        <activity android:name=".activitycodes.CityAndVaccine" />
        <activity android:name=".activitycodes.InformationClinicActivity" />
        <activity android:name=".activitycodes.ConfirmedActivity" />
        <activity android:name=".activitycodes.MainActivity" />
    </application>

</manifest>